#include <stdio.h>
#define LIST_SIZE 10

int main()
{
    int MY_LIST[LIST_SIZE] = {4,5,8,7,9,2,1,3,4,0};

    for (int i = 0; i <= LIST_SIZE; i++)
    {
        for (int k = 0; k <= LIST_SIZE; k++)
        {

        }
    }

    return 0;
}