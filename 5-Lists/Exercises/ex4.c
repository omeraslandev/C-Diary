#include <stdio.h>
#define LIST_SIZE 100

int main()
{
    int i;

    printf("Lütfen bir değer giriniz: ");
    scanf("%d", &i);


    for (int i = 0; i <= LIST_SIZE; i++)
    
    /*
    Baştan yapıcam.
    */
    return 0;
}